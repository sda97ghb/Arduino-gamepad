#ifndef PRESETS_H
#define PRESETS_H

#include "GamepadSettings.h"

const GamepadSettings TerrariaSettings = GamepadSettings(13, 3);
const GamepadSettings HotlimeMiami2Settings = GamepadSettings(25, 3);
const GamepadSettings Left4Dead2Settings = GamepadSettings(80, 3);

#endif // PRESETS_H
